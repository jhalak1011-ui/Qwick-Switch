FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8080

# expand PORT and bind to 0.0.0.0, fallback to 8080
CMD ["sh", "-c", "streamlit run frontend/app.py --server.port=${PORT:-8080} --server.address=0.0.0.0 --server.headless true --server.enableCORS false"]
